#!/bin/sh

OLDPATH="$PATH"

if [ -n "$DMENU_PATH" ]; then
    PATH="$DMENU_PATH:$PATH"
fi

RUN="$(modal-apps | modal -dmenu "$@")"
COMMAND=($RUN)
RUNPATH="$(which "$COMMAND")"

PATH="$OLDPATH"

exec $RUNPATH "${COMMAND[@]:1}"
