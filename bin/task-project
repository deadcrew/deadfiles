#!/bin/bash

set -euo pipefail

root=$(git rev-parse --show-toplevel)
if [[ ! "$root" ]]; then
    echo "unable to find git repository location" >&2
    exit 2
fi


TASKDATA=$root/.task /usr/bin/task "$@"
